1、类型选择器	      h1 {  }	                类型选择器
2、通配选择器	      * {  }	                通配选择器
3、类选择器                   .box {  }	类选择器
4、ID选择器                   #unique { }	ID选择器
5、标签属性选择器	      a[title] {  }	标签属性选择器
6、伪类选择器	      p:first-child { }	伪类
7、伪元素选择器	      p::first-line { }	伪元素
8、后代选择器	      article p	后代运算符
9、子代选择器	      article > p	子代选择器
10、相邻兄弟选择器	      h1 + p	相邻兄弟
11、通用兄弟选择器	      h1 ~ p	通用兄弟


（1）
语法：元素 {样式声明 }

如：
span {
  background-color: DodgerBlue;
  color: #ffffff;
}

（2）
在CSS3中,星号(*)可以和命名空间组合使用:
ns|* - 会匹配ns命名空间下的所有元素
*|* - 会匹配所有命名空间下的所有元素
|* - 会匹配所有没有命名空间的元素

如：
*[lang^=en]{color:green;}

*.warning {color:red;}

*#maincontent {border: 1px solid blue;}

<p class="warning">
  <span lang="en-us">A green span</span> in a red paragraph.
</p>

<p id="maincontent" lang="en-gb">
  <span class="warning">A red span</span> in a green paragraph.
</p>

（3）
语法：.类名 {样式声明 }    等价于    [class~=类名] {样式声明 }

如：
span.classy {
  background-color: DodgerBlue;
}

<span class="classy">Here's a span with some text.</span>

<span>Here's another.</span>

（4）
